input_schema:
  type: object
  properties:
    orders:
      type: array
output_schema:
  type: object
  required: [transformed_orders, total_revenue, total_items, status]
  properties:
    transformed_orders:
      type: array
    total_revenue:
      type: number
    total_items:
      type: integer
    status:
      type: string

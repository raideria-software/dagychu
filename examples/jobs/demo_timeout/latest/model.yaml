template:
  sleep_seconds: 2.0
input_schema:
  type: object
  properties:
    sleep_seconds:
      type: number
output_schema:
  type: object
  required: [sleep_seconds, status]
  properties:
    sleep_seconds:
      type: number
    status:
      type: string
